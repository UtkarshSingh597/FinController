# FinControl Machine Learning Artifacts

This directory stores serialized model weights and preprocessors:
- `isolation_forest_v1.joblib`: Trained Isolation Forest model for multi-tenant payment anomaly scoring.

Generated via `python -m FINCONTROL.ml.training.train_anomaly_detector` or `python FINCONTROL/scripts/train_models.py`.
